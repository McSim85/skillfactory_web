$(document).ready(function() {
	const blockWithId = $('#root'); // ищет по параметру id
	const blockWithClass = $('.block'); // ищет по параметру class
	const spanElement = $('span') // ищет по имени тега

	console.log(blockWithId);
	console.log(blockWithClass);
	console.log(spanElement);
});